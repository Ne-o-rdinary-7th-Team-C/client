type Props = {
  backgroundImageUrl: string;
  questionAuthorName: string;
  questionContent: string;
  userName: string;
  answerContent: string;
};

// 주의사항 : 답장이나 질문이 긴경우 줄바꿈을 통해 구현해주세요
export const QuestionAnswerView = (props: Props) => {
  return <div></div>;
};
